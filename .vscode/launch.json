{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: FastAPI",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "app.main:app",   // ruta al módulo y la instancia FastAPI
                "--reload"
            ],
            "cwd": "${workspaceFolder}", // raíz del proyecto
            "jinja": true
        }
    ]
}
